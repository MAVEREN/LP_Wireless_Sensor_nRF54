/ {
    aliases {
        sensor-power = &sensor_power_gpio;
    };

    sensor_power_gpio: sensor-power-gpio {
        compatible = "gpio-leds";
        gpios = <&gpio0 4 GPIO_ACTIVE_HIGH>;
        label = "Sensor Power Control";
    };
};
